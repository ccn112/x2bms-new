# WEB-02-03 — Phương tiện & thẻ

- **Source image:** `handoff/.../WEB-02_Cư_dân_and_Căn_hộ/images/WEB-02-03__Phương_tiện_and_thẻ.png`
- **Type:** list/dashboard view
- **Render strategy:** Custom Page — `App\Livewire\VehiclesAndCards` (`GET /vehicles-cards`). Livewire + Blade + X2 components.
- **X2 components:** X2AdminShell, X2ActionBar, X2KpiCard ×4, X2SectionCard ×2, X2DataTable ×2, X2StatusBadge.
- **Canonical entities/tables:** `vehicles`, `access_cards`, `apartments`, `residents`, `fee_types`, `fee_rates`.
- **Data source (no hardcode):** KPIs total/cars/motorbikes/`SUM(monthly_fee)`. Vehicles table (plate, type, apartment, parking card, fee, validity). Access cards table (card no, holder, apartment, RFID/biometric, validity, status).
- **Sample data shown:** Biển số, Loại xe, Thẻ ra vào, Sinh trắc, Hiệu lực, Phí gửi xe.
- **Seed:** 108 vehicles (cars 1.2M/mo, motorbikes 120k), 120 access cards (some biometric, some revoked).
- **RBAC/tenancy:** auth + `BelongsToTenant`.
- **Acceptance:** `/vehicles-cards` shows KPIs + vehicles table + access-card table with badges.
- **Screenshot test:** `tests/Browser/web-02.spec.ts` → `WEB-02-03 vehicles & cards`.
