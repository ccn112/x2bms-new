# Deploy `x2.fino.vn` — 2026-08-01 15:00 (gộp 1 lượt)

> **1 file cho lần deploy này** (không tách nhiều file). Đưa live từ commit cũ
> (trước `a5b9fe1`) lên **`4818742`** — gộp tất cả backend mới trong một `git pull`.
> Chi tiết rủi ro từng migration: xem `DEPLOY_20260801_VERIFY_CHECKLIST.md` §1 (giữ nguyên).

## Lần này thêm gì (so với live hiện tại)
- **Cộng đồng GĐ5**: `GET resident/community/bootstrap` (gom tier/feed-scopes/tabs/groups/follows/composer 1 call).
- **P1 Theo dõi dự án**: `resident/me/project-follows` + `operational_project_id` ở card dự án.
- **GĐ9 Mass Seed**: command `community:seed-scale` (chỉ chạy profile `demo` trên dev/live nhỏ; **KHÔNG** `full`/`load`).
- **Cộng đồng GĐ2/3**: group hierarchy (`group_type/scope/capabilities`) + membership grants đa căn.
- **Billing B1–B6**: nhập khoản phí → duyệt/phát hành maker-checker → phân bổ theo dòng/family/ưu tiên.
- **Fix bình luận phiếu** (`4818742`): phiếu gắn qua `resident_id` mở được bình luận (trước 404). Chỉ code, không migration.

> ⚠️ **Weather vẫn `null`** sau deploy này — đó là **thiếu toạ độ dự án trên DB**, không phải code.
> Xử lý riêng (set `projects.latitude/longitude`), không thuộc bước dưới.

## Chạy trên server (CloudPanel site user, trong `<APP_DIR>`)

```bash
cd <APP_DIR>
git remote -v && git branch --show-current            # phải là main
git status --short                                    # PHẢI trống

# 1) Backup DB (BẮT BUỘC — có migration ghi/đổi dữ liệu, xem checklist §1)
DB_NAME=$(grep -E '^DB_DATABASE=' .env | cut -d= -f2)
mysqldump -u <db_user> -p "$DB_NAME" | gzip > ~/x2_backup_pre_20260801_1500.sql.gz
ls -lh ~/x2_backup_pre_20260801_1500.sql.gz           # > 0 byte

# 2) Kéo code + deps
git pull                                              # → 4818742
composer install --no-dev --optimize-autoloader

# 3) Migrate (xem trước danh sách thật)
php artisan migrate:status | grep -i pending
php artisan down --render="errors::503" || true
php artisan migrate --force

# 4) Seed dữ liệu demo (idempotent)
php artisan db:seed --class=ApartmentWalletDemoSeeder
php artisan db:seed --class=ResidentArticleSeeder
php artisan storage:link                              # BẮT BUỘC cho uploads

# 5) Backfill (chạy --dry-run trước rồi chạy thật — idempotent)
php artisan billing:backfill-fee-family --dry-run          && php artisan billing:backfill-fee-family
php artisan billing:backfill-fee-priority --dry-run        && php artisan billing:backfill-fee-priority
php artisan community:backfill-project-follows --dry-run   && php artisan community:backfill-project-follows
php artisan community:backfill-membership-grants --dry-run && php artisan community:backfill-membership-grants

# 6) Dọn cache + bật lại
php artisan optimize:clear
php artisan up
```

## Verify LIVE sau deploy (chạy ở máy bất kỳ)

> Token ở `data.tokens.access_token`, login cần header `X-Device-Id` (checklist cũ ghi `data.token` là SAI).

```bash
BASE=https://x2.fino.vn/api/v1
DEV='X-Device-Id: deploy-verify'
TOKEN=$(curl -s -X POST $BASE/auth/login -H 'Content-Type: application/json' -H "$DEV" \
  -d '{"identifier":"nguyenvananh@gmail.com","password":"Resident@2026!"}' \
  | python3 -c 'import sys,json;print(json.load(sys.stdin)["data"]["tokens"]["access_token"])')
A="Authorization: Bearer $TOKEN"; echo "len ${#TOKEN}"
```

| # | Kiểm | Lệnh | Kỳ vọng |
|---|---|---|---|
| 1 | Bootstrap GĐ5 (mới) | `curl -s -o /dev/null -w '%{http_code}\n' $BASE/resident/community/bootstrap -H "$A" -H "$DEV"` | **200** (trước deploy 404) |
| 2 | Theo dõi dự án (mới) | `curl -s -o /dev/null -w '%{http_code}\n' $BASE/me/project-follows -H "$A" -H "$DEV"` | **200** (KHÔNG có prefix `resident/` — route ở nhóm `me/`) |
| 3 | **Fix bình luận phiếu** | `curl -s -o /dev/null -w '%{http_code}\n' $BASE/resident/payments/10/comments -H "$A" -H "$DEV"` | **200** (trước 404 — đây là bằng chứng `4818742` đã lên) |
| 4 | Bình luận resource lạ | `curl -s -o /dev/null -w '%{http_code}\n' $BASE/resident/foobars/10/comments -H "$A" -H "$DEV"` | 404 (chặn đúng) |
| 5 | Groups vẫn OK | `curl -s -o /dev/null -w '%{http_code}\n' $BASE/resident/community/groups -H "$A" -H "$DEV"` | 200 |

Sau khi xanh: cập nhật `docs/PROGRESS_TRACKER.md` (bootstrap/P1/comments → ✅ + ngày verify) và ghi `DEV_JOURNAL.md`.

## Rollback
- Migration hỏng giữa chừng: phục hồi bằng backup — `gunzip < ~/x2_backup_pre_20260801_1500.sql.gz | mysql -u <db_user> -p <DB_NAME>` (các migration ⚠️ có `down()` trống, xem checklist §1/§6).
- Code hỏng: `git reset --hard 5c89fa8 && composer install --no-dev -o && php artisan optimize:clear` (hoặc commit cũ hơn nếu cần bỏ cả bootstrap).
